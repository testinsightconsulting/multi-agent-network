# Minimal Lab Project

Copy this folder to start a new Cursor-managed lab project.

```bash
cp -r ../templates/.cursor .
cp ../templates/AGENTS.md .
```

Add your topology under `config/` and wire MCP servers in Cursor settings.
